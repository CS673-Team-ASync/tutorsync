
  useEffect( () => {
    getTutorTimes(userId);

    // eslint-disable-next-line
  }, [userId]);  

  const getTutorTimes = async (userId) => {
  
    try {
      var result = await getTutorAvailableTimes(userId);
      console.log(result);
    } catch (err) {
      serverError(err);
      console.log(err);
    }
  }

  const getDate = () => {

    var newDate = new Date().toISOString();
    console.log(newDate);

    // var newMoment = moment().toDate();

    // transmit
    var newMoment = moment().format();
    console.log(newMoment);

    // 
    let birthday = new Date(1995, 11, 17)
    console.log(birthday);

    // 
    let birthday = new Date('1995-12-17T04:00:00')


  }


      <div>
        <div>Test</div>
        <button
          onClick={ () => getTutorTimes(userId)}
        >
          Get Available Times
        </button>
        <button
          onClick={ () => getDate() }
        >
        </button>  
      </div>


/*
// ok css
.dateComponentWrapper {
	width: 550px;
	border: 2px solid black;
}

.dateComponentWrapper > * {
	width: 550px;
}
*/

/* Set width of date component */

/*
.datepicker,
.react-datepicker-wrapper,
.react-datepicker__month-container {
	width: 550px;
	font-size: 1.2rem;
}
*/

/* padding above and below calendar */
/*
.react-datepicker__month-container .react-datepicker__month {
	padding: 1rem 0;
}

.react-datepicker__month-container .react-datepicker__month-text {
	display: inline-block;
	width: 5rem;
	margin: 0.5rem;
	font-size: 1rem;
	padding: 0.5rem !important;
}

.react-datepicker__month-container .react-datepicker__month-text:hover {
	background-color: #534cea;
}

/*
.react-datepicker__current-month {
	font-size: 4rem;
}

.react-datepicker__month {
	padding: 1rem 0;
	text-align: left;
}

.react-datepicker__month .react-datepicker__month-text {
	display: inline-block;
	width: 3rem;
	margin: 2rem;
	font-size: 1rem;
	padding: 0.2rem;

	
	&:hover {
		background-color: #534cea;
	} 
}
*/

/*
.react-datepicker__input-container {
	width: inherit;
}

.react-datepicker-wrapper {
	width: 100%;
}
*/